#!/usr/bin/env ruby

require_relative '../lib/integration_generator'
require_relative '../lib/generator'

parser = IntegrationGenerator::Compiler.new
generator = Generator::Runner
pipeline = Generator::Pipeline.new(compiler: parser, runner_class: generator)
cli = Generator::CLI.new(pipeline:)

exit cli.call(ARGV)
