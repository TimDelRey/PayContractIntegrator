#!/usr/bin/env ruby

require_relative '../lib/integration_generator'
require_relative '../lib/generator'

parser = IntegrationGenerator::Parser.new
generator = Generator::Runner.new
worker = Generator::MainWorker.new(parser:, generator:)

exit worker.call(ARGV)
