#!/usr/bin/env ruby
# frozen_string_literal: true

commands = [
  %w[bundle exec rake test],
  %w[bundle exec rubocop],
  %w[bin/bundler-audit check]
]

commands.each { |command| system(*command, exception: true) }
